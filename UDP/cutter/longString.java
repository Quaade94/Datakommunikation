package cutter;

public class longString {
	
	public String getShortString(){
		String message = "5n compute0 programm2ng, a str3ng is tra4itionally1a sequenc";
		return message;
	}
	
	public String getLongString(){
		String message = "In computer programming, a string is traditionally a sequence of "
				+ "characters, either as a literal constant or as some kind of variable. The "
				+ "latter may allow its elements to be mutated and the length changed, or it "
				+ "may be fixed (after creation). A string is generally understood as a data "
				+ "type and is often implemented as an array of bytes (or words) that stores a"
				+ " sequence of elements, typically characters, using some character encoding. "
				+ "A string may also denote more general arrays or other sequence (or list) data"
				+ " types and structures. In computer programming, a string is traditionally a sequence of "
				+ "characters, either as a literal constant or as some kind of variable. The "
				+ "latter may allow its elements to be mutated and the length changed, or it "
				+ "may be fixed (after creation). A string is generally understood as a data "
				+ "type and is often implemented as an array of bytes (or words) that stores a"
				+ " sequence of elements, typically characters, using some character encoding. "
				+ "A string may also denote more general arrays or other sequence (or list) data"
				+ " types and structures.In computer programming, a string is traditionally a sequence of "
				+ "characters, either as a literal constant or as some kind of variable. The "
				+ "latter may allow its elements to be mutated and the length changed, or it "
				+ "may be fixed (after creation). A string is generally understood as a data "
				+ "type and is often implemented as an array of bytes (or words) that stores a"
				+ " sequence of elements, typically characters, using some character encoding. "
				+ "A string may also denote more general arrays or other sequence (or list) data"
				+ " types and structures.In computer programming, a string is traditionally a sequence of "
				+ "characters, either as a literal constant or as some kind of variable. The "
				+ "latter may allow its elements to be mutated and the length changed, or it "
				+ "may be fixed (after creation). A string is generally understood as a data "
				+ "type and is often implemented as an array of bytes (or words) that stores a"
				+ " sequence of elements, typically characters, using some character encoding. "
				+ "A string may also denote more general arrays or other sequence (or list) data"
				+ " types and structures.In computer programming, a string is traditionally a sequence of "
				+ "characters, either as a literal constant or as some kind of variable. The "
				+ "latter may allow its elements to be mutated and the length changed, or it "
				+ "may be fixed (after creation). A string is generally understood as a data "
				+ "type and is often implemented as an array of bytes (or words) that stores a"
				+ " sequence of elements, typically characters, using some character encoding. "
				+ "A string may also denote more general arrays or other sequence (or list) data"
				+ " types and structures.In computer programming, a string is traditionally a sequence of "
				+ "characters, either as a literal constant or as some kind of variable. The "
				+ "latter may allow its elements to be mutated and the length changed, or it "
				+ "may be fixed (after creation). A string is generally understood as a data "
				+ "type and is often implemented as an array of bytes (or words) that stores a"
				+ " sequence of elements, typically characters, using some character encoding. "
				+ "A string may also denote more general arrays or other sequence (or list) data"
				+ " types and structures.In computer programming, a string is traditionally a sequence of "
				+ "characters, either as a literal constant or as some kind of variable. The "
				+ "latter may allow its elements to be mutated and the length changed, or it "
				+ "may be fixed (after creation). A string is generally understood as a data "
				+ "type and is often implemented as an array of bytes (or words) that stores a"
				+ " sequence of elements, typically characters, using some character encoding. "
				+ "A string may also denote more general arrays or other sequence (or list) data"
				+ " types and structures.In computer programming, a string is traditionally a sequence of "
				+ "characters, either as a literal constant or as some kind of variable. The "
				+ "latter may allow its elements to be mutated and the length changed, or it "
				+ "may be fixed (after creation). A string is generally understood as a data "
				+ "type and is often implemented as an array of bytes (or words) that stores a"
				+ " sequence of elements, typically characters, using some character encoding. "
				+ "A string may also denote more general arrays or other sequence (or list) data"
				+ " types and structures.";
		return message;
		
	}

}
